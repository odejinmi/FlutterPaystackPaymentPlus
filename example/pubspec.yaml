name: flutter_paystack_payment_plus_example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_paystack_payment_plus:
    path: ../
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
